# 評測依賴策略（生成優先）
# 1) 先安裝 root requirements（生成主線）
# 2) 再安裝評測專屬 extras（此檔）
#
# Local build: Build_GenAI.bat 會先呼叫 scripts/setup_env.py，再安裝本檔。
# 預設先嘗試套用 root constraints，若與 GPU profile torch 組合衝突，
# 會自動回退為不套 constraints 的重試流程。
# Docker build: Build_GenAI_Docker.bat 會建置整合服務，Dockerfile 內再安裝本檔

# 評測專屬工具
rich>=13.7.0
matplotlib>=3.7.5
colorama>=0.4.6

# 評測專屬 NLP/NER
gliner>=0.2.22
nltk>=3.8.1
regex>=2023.10.3
ujson>=5.8.0

# 評測專屬語義與校準
sentence-transformers>=2.7.0
einops>=0.7.0
xgboost>=2.0.0

# 評測專屬知識圖譜/外部資料
rdflib>=7.0.0
py2neo>=2021.2.4
requests>=2.32.3
urllib3>=2.0.7,<3

# Dataset helper（評測批次分析工具會使用）
datasets==2.19.2