# --- 注意 ---
# 本檔僅維護套件版本，不在此檔設定 index。
# PyTorch cu128 的來源由 Dockerfile（或安裝指令）單獨處理，
# 以避免影響 torchcodec 等其他套件的來源解析。

# --- 核心 PyTorch 生態（支援 RTX 40/50 系列）---
torch==2.8.0+cu128
torchvision==0.23.0+cu128
torchaudio==2.8.0+cu128
torchcodec==0.7.0

# --- GPTQ 推理核心（請維持此組版本同步）---
# 若版本被覆蓋/漂移，常見錯誤：
# - ImportError: Loading a GPTQ quantized model requires optimum
# - NameError: QuantizeConfig is not defined
# - ImportError: auto_gptq not found
accelerate==1.12.0
diffusers==0.35.1
transformers==4.46.1
tokenizers==0.20.3
safetensors==0.4.3
bitsandbytes>=0.43.0
optimum==1.23.3
optimum-quanto==0.2.7
auto-gptq==0.7.1
exllamav2==0.3.2
peft==0.18.1
ninja>=1.11.1

# --- 非 pip 先決條件（Windows）---
# 1) Visual Studio C++ Build Tools (cl.exe 需可在 PATH 被找到)
# 2) CUDA Toolkit (與已安裝 PyTorch CUDA 版本相容)

# --- 科學運算 / 資料處理 ---
numpy==1.26.4
scipy==1.11.4
scikit-learn==1.3.0
pandas==1.5.3
pyarrow==14.0.2
openpyxl==3.1.2
psutil==5.9.8
py-cpuinfo==9.0.0
nvidia-ml-py==12.535.108

# --- 影像 / 圖像處理 ---
pillow==10.3.0
rembg==2.0.56

# --- NLP / Tokenizer ---
sentencepiece==0.1.99
# Keep protobuf on 3.20.x for sentencepiece tokenizer conversion used by FLUX / SD3.5 pipelines.
protobuf==3.20.3
spacy==3.4.4
spacy-experimental==0.6.4
en-core-web-sm==3.4.0
pyyaml==6.0.3

# NOTE:
# spacy-transformers 1.1.x 會要求 transformers<4.26，與本專案 GPTQ runtime
# 需要的 transformers==4.46.1 衝突。預設流程不依賴 spacy-transformers，
# 若需要 en_core_web_trf，請在獨立 NLP 環境安裝相容版本。
# en-coreference-web-trf 也依賴 spacy-transformers，預設流程同樣不安裝。

# --- 音訊 / 語音 ---
soundfile==0.12.1
ftfy==6.3.1
TTS==0.22.0

# --- 進度列 / 視覺化 / 圖結構 ---
tqdm==4.66.5
plotly==5.20.0
networkx==2.8.8

# --- 其他音訊輔助 ---
pydub==0.25.1

# --- 日文處理 (XTTS Ja Support) ---
cutlet
unidic-lite
fugashi

# 評測依賴策略（�??�優?��?
# 1) ?��?�?root requirements（�??�主線�?
# 2) ?��?裝�?測�?�?extras（此檔�?並以 root requirements ??constraint�?
#
# Local build: Build_GenAI.bat ?��??�叫 scripts/setup_env.py，�?安�??��?
# Docker build: Build_GenAI_Docker.bat ?�建置整?��??��?Dockerfile ?��?安�??��?

# 評測專屬工具
rich>=13.7.0
matplotlib>=3.7.5
colorama>=0.4.6

# 評測專屬 NLP/NER
gliner>=0.2.22
nltk>=3.8.1
regex>=2023.10.3
ujson>=5.8.0

# 評測專屬語義?�校�?
sentence-transformers>=2.7.0
einops>=0.7.0
xgboost>=2.0.0

# 評測專屬?��??��?/外部資�?
rdflib>=7.0.0
py2neo>=2021.2.4
requests>=2.32.3
urllib3>=2.0.7,<3

# Dataset helper（�?測批次�??�工?��?使用�?
datasets==2.19.2

# --- Evaluation / Service runtime ---
fastapi==0.95.2
uvicorn==0.22.0
fastcoref==2.1.6
pydantic==1.10.13
ultralytics>=8.2.0
