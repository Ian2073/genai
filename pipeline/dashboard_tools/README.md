# Dashboard 工具整理區

這個資料夾集中存放歷史上的 dashboard 修補/抽取/檢查腳本與暫存檔，
避免專案根目錄過於雜亂。

## 範圍
- dashboard HTML 修補與格式化腳本
- i18n/導航/模組區塊抽取腳本
- 先前修補流程留下的暫存 HTML/CSS/JS
- dashboard 相關的測試草稿檔

## 注意
- 這些檔案不是主執行入口。
- 核心程式仍在專案根目錄與 `pipeline/`、`runtime/` 等模組，不受影響。
- 若要執行這些腳本，建議在專案根目錄啟動，避免相對路徑不一致。
